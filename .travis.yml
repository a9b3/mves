language: node_js
node_js:
  - "5.1"

cache:
  directories:
    - node_modules

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install silversearcher-ag

install:
  - npm install

script:
  - npm test
